# TIDtoExcel - RFID数据记录系统依赖包
# 完整版本依赖列表，包含所有功能模块

# 核心GUI框架
# tkinter 是Python标准库的一部分，无需单独安装

# Excel文件处理
openpyxl>=3.0.9
# 用于读写Excel文件，支持图片嵌入和格式化

# 图像处理
Pillow>=8.3.2
# 图像处理库，支持多种格式的图片读取、转换和调整大小

# 串口通信
pyserial>=3.5
# RFID设备串口通信，支持Windows/Linux/macOS

# 摄像头和计算机视觉
opencv-python>=4.5.3.56
# 摄像头捕获和图像处理

# OCR文字识别
easyocr>=1.6.2
# 光学字符识别，用于自动识别标签号
# 注意：首次使用时会自动下载模型文件

# 数据处理
numpy>=1.21.0
# 数值计算库，opencv和easyocr的依赖

# 可选依赖（用于增强功能）
# matplotlib>=3.4.3
# 用于数据可视化和图表生成（可选）

# requests>=2.26.0
# HTTP请求库，用于模型下载（easyocr依赖）

# 开发和调试工具（可选）
# pytest>=6.2.5
# 单元测试框架

# black>=21.9b0
# 代码格式化工具

# 系统要求说明：
# - Python 3.7 或更高版本
# - Windows 10/11 (推荐)
# - 至少 2GB 可用内存（用于OCR模型）
# - 至少 1GB 可用磁盘空间（用于OCR模型缓存）

# 安装说明：
# 1. 基础安装：pip install -r requirements.txt
# 2. 如果网络较慢，可以使用国内镜像：
#    pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple/
# 3. 如果遇到依赖冲突，可以尝试最小化安装：
#    pip install -r requirements_minimal.txt

# 注意事项：
# - easyocr首次运行时会下载约100MB的模型文件
# - opencv-python可能需要额外的系统库支持
# - 在某些系统上可能需要安装Microsoft Visual C++ Redistributable